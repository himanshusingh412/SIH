{
  "name": "sih-2026-ai-transformation-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "prisma generate --schema=server/prisma/schema.prisma && npm --prefix server run build && npm --prefix client run build",
    "build": "npm --prefix server install && npm --prefix server run build && npm --prefix client install && npm --prefix client run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
