{
  "name": "sih-2026-ai-transformation-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix server install && npm --prefix server run build && npm --prefix client install && npm --prefix client run build",
    "build": "npm --prefix server install && npm --prefix server run build && npm --prefix client install && npm --prefix client run build"
  }
}
