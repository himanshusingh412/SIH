{
  "name": "sih-2026-ai-transformation-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix client install && prisma generate --schema=server/prisma/schema.prisma && DATABASE_URL=\"file:/tmp/dev.db\" npx prisma db push --schema=server/prisma/schema.prisma --accept-data-loss && npm --prefix server run build && npm --prefix client run build",
    "build": "npm --prefix client install && npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server start",
    "test": "npm --prefix server test"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "docx": "^9.2.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "js-yaml": "^4.1.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "pdfkit": "^0.16.0",
    "pptxgenjs": "^3.12.0",
    "react-markdown": "^10.1.0",
    "xml2js": "^0.6.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/pdfkit": "^0.13.9",
    "@types/xml2js": "^0.4.14",
    "prisma": "^5.22.0"
  }
}
